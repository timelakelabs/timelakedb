[package]
name = "timelake-query"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
datafusion.workspace = true
serde_json.workspace = true
tokio.workspace = true
async-trait = "0.1"
tracing.workspace = true
bytes.workspace = true
timelake-store = { path = "../store" }
timelake-catalog = { path = "../catalog" }
# #57: the last-value cache and its FieldValue type back the last_cache()
# table function, which runs in production, not only tests.
timelake-ingest = { path = "../ingest" }
timelake-lastvalue = { path = "../lastvalue" }

[dev-dependencies]
timelake-buffer = { path = "../buffer" }

