[package]
name = "timelord-query"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
datafusion.workspace = true
serde_json.workspace = true
tokio.workspace = true
async-trait = "0.1"
bytes.workspace = true
timelord-store = { path = "../store" }
timelord-catalog = { path = "../catalog" }

[dev-dependencies]
timelord-buffer = { path = "../buffer" }
timelord-ingest = { path = "../ingest" }
