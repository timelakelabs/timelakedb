# Rust build output (future)
target/

# Release artifacts built by packaging/build.sh — the .deb/.rpm and the
# binary they wrap are attached to a GitHub Release, never committed.
dist/

# Benchmark targets' data volumes — never in git
deploy/compose/data/

# Python
__pycache__/
*.pyc

# AT-7 drill certs are throwaway, minted per run by gen-certs.sh
deploy/compose/tls-drill/certs/

# Brand sheet: a local design asset, not a repo artifact. The palette and
# mark it defined now live in site/assets/ (style.css, logo.svg).
docs/evidence/TimeLoadDB.png

# Scheduled perf-cycle output and the local permission allowlist that
# lets it run unattended (personal, not a repo contract).
ops/logs/
.claude/settings.local.json
/target-linux/
